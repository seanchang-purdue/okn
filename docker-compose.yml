services:
  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    depends_on:
      - client
      - server

  server:
    build:
      context: ./server
    restart: unless-stopped
    expose:
      - "8080"

  client:
    build:
      context: ./client
    restart: unless-stopped
    expose:
      - "4321"
    depends_on:
      - server

networks:
  default:
    name: okn-network